# ⚕️ MediAssist AI Streamlit Secrets Template (TOML Format)
# Rename this file to "secrets.toml" and place it inside the ".streamlit" folder for local testing.
# For Streamlit Community Cloud deployment, paste these values directly into the "Secrets" field in your app settings.

GOOGLE_API_KEY = "your_google_gemini_or_nvidia_api_key_here"
